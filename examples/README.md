# examples

This directory stores sample bookmaker bet inputs and expected matcher outputs for development and regression testing.

## Minimal input example

See `match-lay-input.example.json` for the smallest useful payload the matcher needs to locate an equivalent Betfair lay market.
